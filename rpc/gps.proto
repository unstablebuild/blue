syntax = "proto3";

package rpc;
 
message Coordinates {
	float latitude = 1;
	float longitude = 2;
	float altitude = 3;
    int64 unix_time = 4;
	string deviceID = 5;
}

message Ack {
	int64 unix_time = 1;
}
